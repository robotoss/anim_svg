import Flutter

/// Registration stub for `anim_svg`. All Dart-native communication goes
/// through `dart:ffi` into the bundled `anim_svg_core` xcframework, so this
/// class exists only to satisfy the `pluginClass` declaration in pubspec.yaml
/// and to let Flutter tooling recognise the pod as a plugin.
public class AnimSvgPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {}
}
